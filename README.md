# dependencies

- flask
- python-dotenv

# run

run with `--host=0.0.0.0` for local network testing

### windows

```
> venv\Scripts\activate
> flask run
```

### linux

```
$ source ./venv/bin/activate
$ flask run
```
